import "./chunk-MJOZMRNF.js";

// node_modules/.pnpm/lettersanitizer@1.0.6/node_modules/lettersanitizer/lib/esm/constants.js
var allowedTags = {
  a: ["class", "href", "id", "style", "target"],
  address: ["class", "id", "style"],
  b: ["class", "id", "style"],
  blockquote: ["class", "id", "style"],
  br: ["class", "id", "style"],
  caption: ["class", "id", "style"],
  cite: ["class", "id", "style"],
  code: ["class", "id", "style"],
  col: [
    "align",
    "bgcolor",
    "char",
    "charoff",
    "class",
    "id",
    "style",
    "valign",
    "width"
  ],
  colgroup: [
    "align",
    "bgcolor",
    "char",
    "charoff",
    "class",
    "id",
    "style",
    "valign",
    "width"
  ],
  center: ["class", "id", "style"],
  dd: ["class", "id", "style"],
  div: ["align", "class", "dir", "id", "style"],
  dt: ["class", "id", "style"],
  em: ["class", "id", "style"],
  font: ["class", "color", "face", "id", "size", "style"],
  h1: ["align", "class", "dir", "id", "style"],
  h2: ["align", "class", "dir", "id", "style"],
  h3: ["align", "class", "dir", "id", "style"],
  h4: ["align", "class", "dir", "id", "style"],
  h5: ["align", "class", "dir", "id", "style"],
  h6: ["align", "class", "dir", "id", "style"],
  hr: ["align", "size", "width"],
  i: ["class", "id", "style"],
  img: [
    "align",
    "alt",
    "border",
    "class",
    "height",
    "hspace",
    "id",
    "src",
    "style",
    "title",
    "usemap",
    "vspace",
    "width"
  ],
  label: ["class", "id", "style"],
  legend: ["class", "id", "style"],
  li: ["class", "dir", "id", "style", "type"],
  ol: ["class", "dir", "id", "style", "type", "start", "reversed"],
  p: ["align", "class", "dir", "id", "style"],
  pre: ["class", "id", "style"],
  span: ["class", "id", "style"],
  strong: ["class", "id", "style"],
  style: [],
  sub: ["class", "id", "style"],
  sup: ["class", "id", "style"],
  table: [
    "align",
    "bgcolor",
    "border",
    "cellpadding",
    "cellspacing",
    "class",
    "dir",
    "frame",
    "id",
    "rules",
    "style",
    "width"
  ],
  tbody: ["class", "id", "style"],
  td: [
    "abbr",
    "align",
    "bgcolor",
    "class",
    "colspan",
    "dir",
    "height",
    "id",
    "lang",
    "rowspan",
    "scope",
    "style",
    "valign",
    "width"
  ],
  tfoot: [
    "align",
    "bgcolor",
    "char",
    "charoff",
    "class",
    "id",
    "style",
    "valign"
  ],
  th: [
    "abbr",
    "align",
    "bgcolor",
    "class",
    "colspan",
    "dir",
    "height",
    "id",
    "lang",
    "rowspan",
    "scope",
    "style",
    "valign",
    "width"
  ],
  thead: [
    "align",
    "bgcolol",
    "char",
    "charoff",
    "class",
    "id",
    "style",
    "valign"
  ],
  tr: [
    "align",
    "bgcolor",
    "char",
    "charoff",
    "class",
    "dir",
    "id",
    "style",
    "valign"
  ],
  u: ["class", "id", "style"],
  ul: ["class", "dir", "id", "style"]
};
var removeWithContents = [
  "script",
  "iframe",
  "textarea",
  "title",
  "noscript",
  "noembed",
  "svg"
];
var allowedCssProperties = [
  "azimuth",
  "background",
  "background-blend-mode",
  "background-clip",
  "background-color",
  "background-image",
  "background-origin",
  "background-position",
  "background-position-x",
  "background-position-y",
  "background-repeat",
  "background-repeat-x",
  "background-repeat-y",
  "background-size",
  "border",
  "border-bottom",
  "border-bottom-color",
  "border-bottom-left-radius",
  "border-bottom-right-radius",
  "border-bottom-style",
  "border-bottom-width",
  "border-collapse",
  "border-color",
  "border-left",
  "border-left-color",
  "border-left-style",
  "border-left-width",
  "border-radius",
  "border-right",
  "border-right-color",
  "border-right-style",
  "border-right-width",
  "border-spacing",
  "border-style",
  "border-top",
  "border-top-color",
  "border-top-left-radius",
  "border-top-right-radius",
  "border-top-style",
  "border-top-width",
  "border-width",
  "box-sizing",
  "break-after",
  "break-before",
  "break-inside",
  "caption-side",
  "clear",
  "color",
  "column-count",
  "column-fill",
  "column-gap",
  "column-rule",
  "column-rule-color",
  "column-rule-style",
  "column-rule-width",
  "column-span",
  "column-width",
  "columns",
  "direction",
  "display",
  "elevation",
  "empty-cells",
  "float",
  "font",
  "font-family",
  "font-feature-settings",
  "font-kerning",
  "font-size",
  "font-size-adjust",
  "font-stretch",
  "font-style",
  "font-synthesis",
  "font-variant",
  "font-variant-alternates",
  "font-variant-caps",
  "font-variant-east-asian",
  "font-variant-ligatures",
  "font-variant-numeric",
  "font-weight",
  "height",
  "image-orientation",
  "image-resolution",
  "ime-mode",
  "isolation",
  "layout-flow",
  "layout-grid",
  "layout-grid-char",
  "layout-grid-char-spacing",
  "layout-grid-line",
  "layout-grid-mode",
  "layout-grid-type",
  "letter-spacing",
  "line-break",
  "line-height",
  "list-style",
  "list-style-position",
  "list-style-type",
  "margin",
  "margin-bottom",
  "margin-left",
  "margin-right",
  "margin-top",
  "marker-offset",
  "max-height",
  "max-width",
  "min-height",
  "min-width",
  "mix-blend-mode",
  "object-fit",
  "object-position",
  "opacity",
  "outline",
  "outline-color",
  "outline-style",
  "outline-width",
  "overflow",
  "overflow-x",
  "overflow-y",
  "padding",
  "padding-bottom",
  "padding-left",
  "padding-right",
  "padding-top",
  "page-break-after",
  "page-break-before",
  "page-break-inside",
  "pause",
  "pause-after",
  "pause-before",
  "pitch",
  "pitch-range",
  "quotes",
  "richness",
  "speak",
  "speak-header",
  "speak-numeral",
  "speak-punctuation",
  "speech-rate",
  "stress",
  "table-layout",
  "text-align",
  "text-align-last",
  "text-autospace",
  "text-combine-upright",
  "text-decoration",
  "text-decoration-color",
  "text-decoration-line",
  "text-decoration-skip",
  "text-decoration-style",
  "text-emphasis",
  "text-emphasis-color",
  "text-emphasis-style",
  "text-indent",
  "text-justify",
  "text-kashida-space",
  "text-orientation",
  "text-overflow",
  "text-transform",
  "text-underline-position",
  "text-wrap",
  "text-wrap-mode",
  "text-wrap-style",
  "unicode-bidi",
  "vertical-align",
  "voice-family",
  "white-space",
  "white-space-collapse",
  "width",
  "word-break",
  "word-spacing",
  "word-wrap",
  "writing-mode",
  "zoom"
];

// node_modules/.pnpm/lettersanitizer@1.0.6/node_modules/lettersanitizer/lib/esm/index.js
function prependIdToSelectorText(selectorText, id) {
  if (!id)
    return selectorText;
  return selectorText.split(",").map((selector) => selector.trim()).map((selector) => {
    const s = selector.replace(/\./g, "." + id + "_").replace(/#/g, "#" + id + "_");
    if (s.toLowerCase().startsWith("body")) {
      return "#" + id + " " + s.substring(4);
    } else {
      return "#" + id + " " + s;
    }
  }).join(",");
}
function sanitizeCssValue(cssValue, allowedSchemas, rewriteExternalResources) {
  return cssValue.trim().replace(/expression\((.*?)\)/g, "").replace(/url\(["']?(.*?)["']?\)/g, (match, url) => {
    if (rewriteExternalResources) {
      return `url("${encodeURI(rewriteExternalResources(decodeURI(url)))}")`;
    } else if (allowedSchemas.includes(url.toLowerCase().split(":")[0])) {
      return match;
    } else {
      return "";
    }
  });
}
function sanitizeCssStyle(style, allowedSchemas, allowedCssProperties3, preserveCssPriority, rewriteExternalResources) {
  if (!style) {
    return;
  }
  const properties = [];
  for (let i = 0; i < style.length; i++) {
    const name = style[i];
    properties.push(name);
  }
  for (const name of properties) {
    if (allowedCssProperties3.includes(name)) {
      const value = style.getPropertyValue(name);
      style.setProperty(name, sanitizeCssValue(value, allowedSchemas, rewriteExternalResources), preserveCssPriority ? style.getPropertyPriority(name) : void 0);
    } else {
      style.removeProperty(name);
    }
  }
}
function sanitizeCssRule(rule, id, allowedSchemas, allowedCssProperties3, preserveCssPriority, rewriteExternalResources) {
  rule.selectorText = prependIdToSelectorText(rule.selectorText, id);
  sanitizeCssStyle(rule.style, allowedSchemas, allowedCssProperties3, preserveCssPriority, rewriteExternalResources);
}
var defaultAllowedSchemas = ["http", "https", "mailto"];
function sanitizeHtml(input, { dropAllHtmlTags = false, rewriteExternalLinks, rewriteExternalResources, id = "msg_" + String.fromCharCode(...new Array(24).fill(void 0).map(() => Math.random() * 25 % 25 + 65)), allowedSchemas = defaultAllowedSchemas, allowedCssProperties: allowedCssProperties3 = allowedCssProperties, preserveCssPriority = true, noWrapper = false }) {
  var _a, _b, _c, _d, _e;
  if (noWrapper)
    id = "";
  const doc = new DOMParser().parseFromString(input, "text/html");
  allowedSchemas = Array.isArray(allowedSchemas) ? allowedSchemas.map((schema) => schema.toLowerCase()) : defaultAllowedSchemas;
  const commentIter = doc.createNodeIterator(doc.documentElement, NodeFilter.SHOW_COMMENT);
  let node;
  while (node = commentIter.nextNode()) {
    (_a = node.parentNode) == null ? void 0 : _a.removeChild(node);
  }
  const removeTags = [...removeWithContents];
  if (dropAllHtmlTags) {
    removeTags.push("style");
  }
  const disallowedList = doc.querySelectorAll(removeTags.join(", "));
  disallowedList.forEach((element) => element.remove());
  const styleList = doc.querySelectorAll("head > style");
  styleList.forEach((element) => {
    doc.body.appendChild(element);
  });
  const toRemove = [];
  const elementIter = doc.createNodeIterator(doc.body, NodeFilter.SHOW_ELEMENT, {
    acceptNode: () => {
      return NodeFilter.FILTER_ACCEPT;
    }
  });
  while (node = elementIter.nextNode()) {
    const element = node;
    const tagName = element.tagName.toLowerCase();
    if (tagName === "body" || tagName === "html") {
      continue;
    }
    if (dropAllHtmlTags) {
      if (node.textContent) {
        const textNode = doc.createTextNode(node.textContent);
        (_b = node.parentNode) == null ? void 0 : _b.replaceChild(textNode, node);
      } else {
        (_c = node.parentNode) == null ? void 0 : _c.removeChild(node);
      }
      continue;
    }
    if (tagName in allowedTags) {
      const allowedAttributes = allowedTags[tagName];
      for (const attribute of element.getAttributeNames()) {
        if (!allowedAttributes.includes(attribute)) {
          element.removeAttribute(attribute);
        } else if (attribute === "class" && !noWrapper) {
          element.setAttribute(attribute, ((_d = element.getAttribute(attribute)) == null ? void 0 : _d.split(" ").map((className) => id + "_" + className).join(" ")) ?? "");
        } else if (attribute === "id" && !noWrapper) {
          element.setAttribute(attribute, id + "_" + (element.getAttribute(attribute) ?? ""));
        } else if (attribute === "href" || attribute === "src") {
          const value = element.getAttribute(attribute) ?? "";
          if (attribute === "href" && rewriteExternalLinks) {
            element.setAttribute(attribute, rewriteExternalLinks(value));
          } else if (attribute === "src" && rewriteExternalResources) {
            element.setAttribute(attribute, rewriteExternalResources(value));
          } else if (!allowedSchemas.includes(value.toLowerCase().split(":")[0])) {
            element.removeAttribute(attribute);
          }
        }
      }
      sanitizeCssStyle(element.style, allowedSchemas, allowedCssProperties3, preserveCssPriority, rewriteExternalResources);
      if (tagName === "a") {
        element.setAttribute("rel", "noopener noreferrer");
        element.setAttribute("target", "_blank");
      }
    } else {
      element.insertAdjacentHTML("afterend", element.innerHTML);
      toRemove.push(element);
    }
  }
  for (const element of toRemove) {
    try {
      try {
        (_e = element.parentNode) == null ? void 0 : _e.removeChild(element);
      } catch {
        element.outerHTML = "";
      }
    } catch {
      try {
        element.remove();
      } catch {
      }
    }
  }
  const bodyStyleList = doc.querySelectorAll("body style");
  bodyStyleList.forEach((element) => {
    const styleElement = element;
    const stylesheet = styleElement.sheet;
    const newRules = [];
    if (!stylesheet.cssRules) {
      styleElement.textContent = "";
      return;
    }
    for (let i = 0; i < stylesheet.cssRules.length; i++) {
      const rule = stylesheet.cssRules[i];
      if ("selectorText" in rule) {
        sanitizeCssRule(rule, id, allowedSchemas, allowedCssProperties3, preserveCssPriority, rewriteExternalResources);
        newRules.push(rule);
      } else if ("cssRules" in rule && "media" in rule) {
        const mediaRule = rule;
        const newRulesMedia = [];
        for (let i2 = 0; i2 < mediaRule.cssRules.length; i2++) {
          const rule2 = mediaRule.cssRules[i2];
          if (rule2.type === rule2.STYLE_RULE) {
            sanitizeCssRule(rule2, id, allowedSchemas, allowedCssProperties3, preserveCssPriority, rewriteExternalResources);
            newRulesMedia.push(rule2);
          }
        }
        while (mediaRule.cssRules.length > 0) {
          mediaRule.deleteRule(0);
        }
        for (const rule2 of newRulesMedia) {
          mediaRule.insertRule(rule2.cssText, mediaRule.cssRules.length);
        }
        newRules.push(mediaRule);
      }
    }
    styleElement.textContent = newRules.map((rule) => rule.cssText).join("\n");
  });
  if (noWrapper) {
    return doc.body.innerHTML;
  } else {
    const div = doc.createElement("div");
    div.id = id;
    div.innerHTML = doc.body.innerHTML;
    return div.outerHTML;
  }
}
function sanitizeText(text) {
  const div = document.createElement("div");
  div.textContent = text;
  return div.innerHTML;
}
function sanitize(html, text, options) {
  let contents = html ?? "";
  if ((contents == null ? void 0 : contents.length) === 0 && text) {
    contents = sanitizeText(text).split("\n").map((line) => "<p>" + line + "</p>").join("\n");
  }
  return sanitizeHtml(contents, options ?? {});
}
var allowedCssProperties2 = allowedCssProperties;
export {
  allowedCssProperties2 as allowedCssProperties,
  sanitize
};
//# sourceMappingURL=lettersanitizer.js.map
